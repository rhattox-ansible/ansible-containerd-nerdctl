- name: ContainerD NerdCTL Installation
  hosts: localhost
  become: true
  gather_facts: true

  vars_files:
    - defaults/main.yaml

  tasks:
    - name: Run the nerdctl installation workflow
      ansible.builtin.include_tasks: "tasks/main.yaml"
